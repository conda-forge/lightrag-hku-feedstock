schema_version: 1

context:
  name: lightrag-hku
  version: 1.4.16

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/lightrag_hku-${{ version }}.tar.gz
  sha256: 4453582e2c82f91c7c675aa3e09939b8a66816775da3370cb11eea94776aa77e
  patches:
    - requirements.patch

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - tiktoken
    - networkx
    - nano-vectordb
    - tenacity
    - configparser
    - pypinyin
    - xlsxwriter >=3.1.0
    - google-api-core >=2.0.0,<3.0.0
    - aiohttp
    - numpy >=1.24.0,<3.0.0
    - python-dotenv
    - setuptools
    - pipmaster
    - packaging
    - json-repair
    - pydantic
    - google-genai >=1.0.0,<2.0.0
    - pandas >=2.0.0,<2.4.0
tests:
  - python:
      imports:
        - lightrag
      pip_check: true

      python_version: ${{ python_min }}.*
about:
  summary: 'LightRAG: Simple and Fast Retrieval-Augmented Generation'
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/HKUDS/LightRAG

extra:
  recipe-maintainers:
    - jan-janssen
